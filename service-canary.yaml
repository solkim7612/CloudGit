apiVersion: v1
kind: Service
metadata:
  name: my-calc-service
  namespace: metallb-system
spec:
  type: LoadBalancer
  selector:
    app: my-calc
    # color: blue  <-- 이 부분이 아예 없어야 함
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
